//- Local variables
- const fullYear = (new Date).getFullYear();

//- External variables
block variables

doctype html
html(lang='en')
  head
    //- Meta
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1, maximum-scale=1')
    meta(property='description' content='Hello world')

    //- Favicons
    //- link(rel='icon' href=require('../assets/img/favicon.svg') type='image/svg+xml' sizes='any')

    //- Page title
    title= title

    //- Page styles
    block style
      //- common used styles
      //- link(rel='stylesheet' href=require('./ui-kit-layout.scss'))

    block head
      //- script(src=require('Scripts/common.js') defer)

      style=require('./ui-kit-layout.scss')

  body
    .header-container
      .header-content
        a(href="index.html").header-left
          //- img.app-logo(src=require('../assets/img/favicon.svg'))
          span App demo
        .header-middle
          a(href="index.html") Home
          a(href="contact.html") Contact
          a(href="about.html") About
        .header-right
          span.glyphicon.glyphicon-user.mr-m
          span Login

    //- Main content
    main.main-container
      .main-content
        block content

    //- Main footer
    .footer-container
      div.
        Copyleft 
        Powered by 

    //- Page scripts
    block script

mixin button-main(props={})
  -
    const {
      text='click me',
      size='medium',
      hovered=false,
      link='',
    } = props

  +button({
    variant: 'main',
    size: size,
    hovered: hovered,
    link: link,
    })
      +text({
        h3: text,
        color: 'white',
      })
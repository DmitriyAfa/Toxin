mixin simple-button(props={})
  -
    const {
      text='click me',
      hovered=false,
      hoveringColor='gray',
      color='primary',
      name=undefined,
    } = props

  +button({
    hovered: hovered,
    variant: 'simple',
    size: undefined,
    name: name,
  })
    +text({
      h3: text,
      color: color,
      hoveringColor: hoveringColor,
      hovered: hovered,
    })
mixin big-arrow-button(props={})
  -
    const {
      text='перейти к оплате',
      size='medium',
    } = props

  +button({
    variant: 'main',
    size: 'biggest',
    hasArrow: true,
  })
    +text({
      h3: text,
      color: 'white',
    })